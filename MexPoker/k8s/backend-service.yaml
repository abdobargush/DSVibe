apiVersion: v1
kind: Service
metadata:
  name: poker-backend
  labels:
    app: poker-app
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: poker-app
    component: backend
