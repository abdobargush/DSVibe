apiVersion: v1
kind: Service
metadata:
  name: envoy
  labels:
    app: temperature-converter
    component: envoy
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: temperature-converter
    component: envoy
