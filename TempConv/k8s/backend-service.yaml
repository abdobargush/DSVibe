apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: temperature-converter
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
  selector:
    app: temperature-converter
    component: backend
